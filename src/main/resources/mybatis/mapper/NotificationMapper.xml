<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.airconditioner.community.mapper.NotificationMapper">
    <insert id="insert">


        insert into notification (id, notifier, receiver, outerid, type, gmt_create, status, notifier_name, outer_title)
        values (#{id}, #{notifier}, #{receiver}, #{outerid}, #{type}, #{gmtCreate}, #{status}, #{notifierName},
                #{outerTitle})
    </insert>

    <select id="countNotificationByUserId" resultType="Integer">
        select count(*)
        from notification
        where receiver = #{userId}
    </select>

    <select id="countUnreadNotificationByUserId" resultType="Integer">
        select count(*)
        from notification
        where receiver = #{userId}
          and status = #{status}
    </select>

    <select id="selectNotificationByUserId" resultType="com.airconditioner.community.bean.Notification">
        select *
        from notification
        where receiver = #{userId}
        order by gmt_create desc
            limit #{offset}
               , #{size}

    </select>

    <select id="selectNotificationById" resultType="com.airconditioner.community.bean.Notification">
        select *
        from notification
        where id = #{id}
    </select>

    <update id="updateStatusById">
        update notification
        set status = #{status}
        where id = #{id}
    </update>
</mapper>
